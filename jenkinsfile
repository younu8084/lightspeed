  pipeline {
    agent any
    options { timeout(time: 20, unit: 'MINUTES') }
    stages {
     
      stage('Run .sh') {
        steps{
        echo "run entrypoint script"
        ./entrypoint.sh
        }
      }
    }
}

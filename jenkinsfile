  pipeline {
    agent any
    options { timeout(time: 20, unit: 'MINUTES') }
    stages {
     
      stage('Run sh file') {
        steps{
        sh('sudo ./entrypoint.sh')
        }
      }
    }
}

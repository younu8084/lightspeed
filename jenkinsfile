  pipeline {
    agent any
    options { timeout(time: 20, unit: 'MINUTES') }
    stages {
     
      stage('Run sh file') {
        steps{
        sh(' chmod  777 ./entrypoint.sh')
        sh('./entrypoint.sh')
        }
      }
    }
}
